Return 400 instead of 500 for invalid Julian Date values in the `epoch`, `mid_obs_time_jd`, and `start_time_jd` parameters, so malformed client input is reported as a client error rather than an unhandled server error.
