Return HTTP 400 instead of a 500 database error when a non-integer NORAD ID is supplied to the tools or ephemeris endpoints (`id` with `id_type=catalog`, the names-from-norad-id endpoint, and the `norad_id` parameter).
