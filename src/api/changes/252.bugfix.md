Return HTTP 400 instead of 500 when the `catalog` parameter on the ephemeris catalog-number endpoints is not a valid integer NORAD ID, and made the global error handler resilient to exceptions (such as SQLAlchemy errors) whose `code` attribute is not an HTTP status code.
