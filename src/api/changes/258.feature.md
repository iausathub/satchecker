Added a `/tools/ephemeris-data-at-epoch/` endpoint that returns the raw operator-provided ephemeris (all stored points) for every satellite with coverage at a given epoch, as a compact Parquet file or a zip of per-satellite CSV files. Currently limited to Starlink satellites.
